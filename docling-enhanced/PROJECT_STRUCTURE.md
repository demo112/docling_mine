# 项目结构说明

## 📁 目录结构

```
docling-enhanced/
├── README.md              # 项目说明文档
├── PROJECT_STRUCTURE.md   # 项目结构说明（本文件）
├── requirements.txt       # Python依赖包列表
├── run.py                 # 启动脚本
├── docling_ui.py         # 增强版Streamlit界面
├── test_document.md      # 测试文档1
└── test_multipage.md     # 测试文档2（多页）
```

## 🔧 文件说明

### 核心文件

- **`docling_ui.py`**: 增强版Streamlit用户界面
  - 实时进度显示
  - 页面级进度跟踪
  - 批量下载功能
  - 错误处理和用户提示

- **`run.py`**: 便捷启动脚本
  - 自动检查依赖
  - 启动Streamlit服务
  - 错误处理

### 配置文件

- **`requirements.txt`**: Python依赖包列表
  - 核心依赖：docling, streamlit
  - 图像处理：Pillow
  - 数据处理：pandas

### 文档文件

- **`README.md`**: 项目主要说明文档
- **`PROJECT_STRUCTURE.md`**: 项目结构详细说明

### 测试文件

- **`test_document.md`**: 基础测试文档
- **`test_multipage.md`**: 多页测试文档

## 🚀 使用方法

### 方法1: 使用启动脚本（推荐）
```bash
cd docling-enhanced
python run.py
```

### 方法2: 直接使用Streamlit
```bash
cd docling-enhanced
streamlit run docling_ui.py
```

### 方法3: 自定义端口
```bash
cd docling-enhanced
streamlit run docling_ui.py --server.port 8502
```

## 🔄 与原项目的关系

### 独立性
- ✅ 完全独立的项目目录
- ✅ 独立的依赖管理
- ✅ 独立的启动方式
- ✅ 不影响原项目代码

### 依赖关系
- 依赖原项目的 `docling` 核心库
- 通过pip安装docling包来使用核心功能
- 界面层完全独立开发

### 更新策略
- 原项目更新时，只需更新docling包版本
- 界面增强功能独立维护
- 可以选择性合并原项目的新功能

## 📦 部署建议

### 开发环境
```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows

# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行项目
python run.py
```

### 生产环境
```bash
# 使用Docker部署（可选）
# 或直接在服务器上运行
nohup python run.py &
```

## 🔧 自定义配置

### 修改端口
编辑 `run.py` 文件中的端口设置：
```python
"--server.port", "8501"  # 改为其他端口
```

### 修改界面
直接编辑 `docling_ui.py` 文件来自定义界面功能。

### 添加功能
在 `docling_ui.py` 中添加新的功能模块。